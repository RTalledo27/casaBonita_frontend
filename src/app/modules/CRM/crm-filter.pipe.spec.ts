import { CrmFilterPipe } from './crm-filter.pipe';

describe('CrmFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CrmFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
