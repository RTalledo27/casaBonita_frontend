import { ServiceDeskFilterPipe } from './service-desk-filter.pipe';

describe('ServiceDeskFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceDeskFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
