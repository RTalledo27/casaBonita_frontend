import { EstadoTicketPipe } from './estado-ticket.pipe';

describe('EstadoTicketPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoTicketPipe();
    expect(pipe).toBeTruthy();
  });
});
